# groovyexec.py

This script was written up for arbitrary command execution via the Jenkins API. 

# spider_parser.py
This script was written to parse the output files from nxc's spider_plus module. It also allows you to search the index of the shares from the spider_plus json files. Additionally, if you specify a username and password it will generate the comamnd needed to mount the drive over CIFS. 

